spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/accountmanger
    username: root
    password: root
  session:
    timeout: 86400
    store-type: redis
  redis:
    port: 6379
    host: 127.0.0.1
    database: 1
logging:
  level:
    cn:
      rzpt:
        mapper: trace
        service: info
        controller: info